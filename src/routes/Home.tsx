import styled from "styled-components";
import Layout from "../components/Layout";

const Home = () => {
  return (
    <Layout showHeader={true}>
      <h1>home</h1>
    </Layout>
  );
};
export default Home;
